version: '3.8'

services:
  web:
    build: .
    container_name: mi-app-web
    ports:
      - "80:80"
    restart: always
    environment:
      # IMPORTANTE: Estas variables son leídas por src/db.php
      - DB_HOST=${DB_HOST}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
